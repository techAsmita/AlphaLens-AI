"""
Domain / persistence models.

Empty for now — this step establishes the project structure only. Once
a database is introduced (e.g. SQLAlchemy or an ORM of choice), table
and domain models live here, separate from the Pydantic request/response
schemas in ``app.schemas``.
"""
